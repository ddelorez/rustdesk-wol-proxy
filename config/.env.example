# RustDesk WOL Proxy - Environment Configuration Template
# Copy this file to .env and fill in your actual values

# ==============================
# REQUIRED CONFIGURATION
# ==============================

# API Authentication Key
# - Minimum 20 characters for security
# - Maximum 256 characters
# - Recommended: Use python3 -c "import secrets; print('wol_prod_' + secrets.token_hex(16))"
# - This key must be provided in all API requests via the 'key' query parameter
WOL_API_KEY="wol_prod_your_strong_random_key_here_min_20_chars"

# ==============================
# OPTIONAL CONFIGURATION
# ==============================

# Broadcast IP Address for WOL Packets
# - Default: 10.10.10.255
# - Must be a valid IPv4 address
# - Typically the broadcast address of your network subnet
# - Example: For 192.168.1.0/24 network, use 192.168.1.255
BROADCAST_IP="10.10.10.255"

# Log File Path
# - Default: /var/log/rustdesk-wol-proxy.log
# - Path where application logs will be written
# - Directory will be created if it doesn't exist
# - For development, use: ./dev.log
# - For production, use: /var/log/rustdesk-wol-proxy.log
LOG_FILE="/var/log/rustdesk-wol-proxy.log"

# ==============================
# Device ID to MAC Address Mapping
# ==============================
# NOTE: Currently configured in src/app.py ALLOWED_IDS dictionary
# 
# Device Configuration Format:
# "RustDesk_ID": "MAC:ADDRESS:FORMAT"
#
# Example mapping:
# {
#     "123456789": "AA:BB:CC:DD:EE:FF",    # Desktop PC
#     "987654321": "11:22:33:44:55:66",    # Laptop
# }
#
# To add devices:
# 1. Edit src/app.py
# 2. Add entries to ALLOWED_IDS dictionary  
# 3. Restart the service
# 4. Test with: curl "http://localhost:5001/health"

# ==============================
# NOTES FOR PRODUCTION DEPLOYMENT
# ==============================
#
# 1. API Key Security:
#    - Generate with: python3 -c "import secrets; print('wol_prod_' + secrets.token_hex(16))"
#    - Store securely (not in git)
#    - Consider using systemd EnvironmentFile directive
#
# 2. Broadcasting:
#    - Ensure BROADCAST_IP matches your network configuration
#    - Target devices must have WOL enabled in BIOS
#    - Must be on the same network segment
#
# 3. Logging:
#    - Ensure LOG_FILE directory is writable by the app process
#    - Log files rotate: 5MB max per file, 3 backup files kept
#    - Check logs for troubleshooting: tail -f LOG_FILE
#
# 4. Service Setup:
#    - Use systemd service file: config/rustdesk-wol.service
#    - Load environment from this file: EnvironmentFile=config/.env
#    - Verify with: systemctl status rustdesk-wol
#
# 5. Testing:
#    - Test with health endpoint: curl http://localhost:5001/health
#    - Full test suite: python3 tests/run_all_tests.py
